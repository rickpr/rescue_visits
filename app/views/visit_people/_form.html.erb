<%= form_for(@visit_person) do |f| %>
  <% if @visit_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visit_person.errors.count, "error") %> prohibited this visit_person from being saved:</h2>

      <ul>
      <% @visit_person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :visit_id %><br>
    <%= f.text_field :visit_id %>
  </div>
  <div class="field">
    <%= f.label :person_id %><br>
    <%= f.text_field :person_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
