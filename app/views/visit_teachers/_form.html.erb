<%= form_for(@visit_teacher) do |f| %>
  <% if @visit_teacher.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visit_teacher.errors.count, "error") %> prohibited this visit_teacher from being saved:</h2>

      <ul>
      <% @visit_teacher.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :visit_id %><br>
    <%= f.text_field :visit_id %>
  </div>
  <div class="field">
    <%= f.label :teacher_id %><br>
    <%= f.text_field :teacher_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
